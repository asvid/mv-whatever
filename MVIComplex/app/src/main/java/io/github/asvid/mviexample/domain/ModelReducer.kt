package io.github.asvid.mviexample.domain

import io.github.asvid.mviexample.redux.Reducer

class ModelReducer: Reducer<ModelState, ModelAction> {
    override fun reduce(currentState: ModelState, action: ModelAction): ModelState {
        TODO("Not yet implemented")
    }
}